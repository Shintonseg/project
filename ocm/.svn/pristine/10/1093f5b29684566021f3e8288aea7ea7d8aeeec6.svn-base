package com.tible.ocm.services;

import com.tible.ocm.models.mongo.SynchronizedDirectory;

public interface SynchronizedDirectoryService {

    boolean existsByName(String name);

    SynchronizedDirectory findByName(String name);

    SynchronizedDirectory save(SynchronizedDirectory synchronizedDirectory);
}
